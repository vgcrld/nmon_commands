#!/usr/bin/env ruby

require 'nmon_commands'
require 'awesome_print'


opts = Options.new

files = NmonCommands.get_file_list(opts.customer, opts.uuid)

filtered = NmonCommands.filter_by_dates(files,opts.start,opts.end)
